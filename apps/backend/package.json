{
  "name": "sf-decision-platform",
  "version": "1.0.0",
  "description": "SF serverless decision platform - Node.js Lambda + API Gateway",
  "main": "src/handler.js",
  "type": "commonjs",
  "scripts": {
    "deploy": "serverless deploy",
    "remove": "serverless remove",
    "offline": "serverless offline",
    "lint": "node -c src/handler.js",
    "test": "node --test src/engine/*.test.js src/rules/*.test.js src/admin.test.js"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0"
  },
  "devDependencies": {
    "serverless": "^3.38.0",
    "serverless-offline": "^13.6.0"
  }
}
